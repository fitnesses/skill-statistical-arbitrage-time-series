# zeus MCP 连接信息（复制为 .env 后填写；.env 已被 .gitignore 忽略，不会提交）
# 放在你的工作目录（只对该目录生效），或放在 skill 目录（对所有项目生效）。
# 若 Claude Code 里已添加名为 zeus 的 MCP server，可以不建 .env，脚本会自动复用那里的地址和 token。
ZEUS_MCP_URL=http://<host>:8000/mcp
ZEUS_MCP_TOKEN=<your-token>
